// Package hint provides the small set of string-matching
// helpers used to build "did you mean?" suggestions from
// typos. It is a dependency-free leaf package so the
// parser and the resolver can both lean on it without
// risking an import cycle.
package hint
